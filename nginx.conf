server {
    listen 80;
    server_name www.d2km.online d2km.online;

    root /var/www/blog/dist;
    index index.html;

    # Redirect www to non-www (optional)
    # if ($host = www.d2km.online) {
    #     return 301 https://d2km.online$request_uri;
    # }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # SPA routing - all routes go to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
    }
}

# HTTPS configuration (after installing SSL certificate)
# server {
#     listen 443 ssl http2;
#     server_name www.d2km.online d2km.online;
#
#     ssl_certificate /etc/letsencrypt/live/d2km.online/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/d2km.online/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     root /var/www/blog/dist;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
#         expires 1y;
#         add_header Cache-Control "public, immutable";
#     }
# }
#
# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name www.d2km.online d2km.online;
#     return 301 https://$server_name$request_uri;
# }
